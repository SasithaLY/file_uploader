<% 
var name = name;
var photo = photo;
var success = success;
var auth = auth;

%>
<%- include('header', {auth:auth}); -%>

    <div class="container mt-5">
        <div class="jumbotron text-center">
            <h2>Upload Files To Google Drive</h2>
            <div class="mt-3">
                <a href="<%=url%>">
                    <button class="btn btn-light px-3"><img class="mx-2" src="./google.png" width="32"
                            height="32" />Sign in
                        with Google</button>
                </a>
            </div>

        </div>
    </div>

    <%- include('footer'); -%>